# OpenAI API key (optional — without it, the AI falls back to deterministic responses)
# Get your key at https://platform.openai.com/api-keys
# OPENAI_API_KEY=sk-your-key-here

# Server port
PORT=3001
